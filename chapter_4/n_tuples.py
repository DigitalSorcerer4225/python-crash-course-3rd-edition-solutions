# What are tuples and why are they immutable in python?
# What are the use cases for tuples?
# Technically, you cannot modify a tupple. However, you can "modify" a tupple by assigning a "modified version" to the variable that referred to the original tupple object.


#Tuples are technically defined by the presence of a comma; the parentheses make them  look neater and more readable.
topple = 1,3
print(topple)